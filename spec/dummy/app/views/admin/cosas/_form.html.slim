/ # locals: (object: nil, asociable: false)

div style="max-width: 300px" data-controller="cosa"
  = pg_form_for(@cosa || object) do |f|
    = f.mensajes_de_error

    = hidden_field_tag :asociable, true if asociable
    = f.input :nombre
    = f.input :tipo
    = f.pg_associable :categoria_de_cosa
    .mt-2
    = f.button :submit
