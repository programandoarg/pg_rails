- content_for(:title, <%= class_name %>.model_name.human.pluralize)
- content_for :actions do
  = link_to new_<%= singular_table_name %>_path, class: 'btn btn-primary' do 
    span.fa.fa-plus
    |  Crear #{<%= class_name %>.model_name.human.downcase}
  .btn.btn-warning.exportar data-url="#{<%= plural_table_name %>_path(format: 'xlsx')}"
    span.fa.fa-list
    |  Exportar
.form-wrapper
  = smart_listing_controls_for(:<%= plural_table_name %>) do |c|
    = @filtros.filtros_html

  .card.float-e-margins
    .card-title
      h5 Listado de #{<%= class_name %>.model_name.human.pluralize.downcase}
    .card-body
      = smart_listing_render(:<%= plural_table_name %>)
